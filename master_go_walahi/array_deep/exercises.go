package main

/*import (
	"fmt"
	"strings"
)

func main() {
	var cities [2]string
	fmt.Println(len(cities))

	grades := [3]float64{1, 2}
	fmt.Println(grades)

	taskDone := [...]bool{}
	fmt.Println(taskDone)

	fmt.Println(strings.Repeat("#", 25))

	//using classical for to iterate
	for i := 0; i < len(cities); {
		fmt.Println("index", i, "value", cities[i])
		i++
	}
	//using range to iterate
	for index, value := range grades {
		fmt.Println("index:", index, "value:", value)
	}

	nums := [...]int{30, -1, -6, 90, -6}
	fmt.Println(len(nums))

	for j := 0; j < len(nums); j++ {
		if nums[j] > 0 && nums[j]%2 == 0 && j < (len(nums)) {
			fmt.Println(nums[j])
			j++

		}
	}

}*/
